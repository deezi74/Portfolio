# Local machine/SDK config - never commit, path is different for everyone
local.properties

# Build output
build/
.gradle/
*.apk
*.aab

# Native build intermediates - this is also where CMake's FetchContent
# clones llama.cpp's source to compile against (see cpp/CMakeLists.txt) -
# never commit that, it's ~170MB and gets re-fetched on demand.
.cxx/

# Android Studio
.idea/
*.iml
.DS_Store
captures/
